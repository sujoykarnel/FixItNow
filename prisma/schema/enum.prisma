enum Role {
    ADMIN
    CUSTOMER
    TECHNICIAN
}

enum ActiveStatus {
    ACTIVE
    BLOCKED
}

enum BookingStatus {
    REQUESTED
    ACCEPET
    DECLINED
    IN_PROGRESS
    CANCELED
    COMPLETED
}

enum PaymentProvider {
    STRIPE
    SSLCOMMERZ
}

enum PaymentStatus {
    PENDING
    SUCCESS
    FAILED
    REFUNDED
}