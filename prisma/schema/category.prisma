model Category {
    id String @id @default(uuid())
    name String @unique
    discription String?
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt

    // service
    services Service[]

    @@map("categories")
}